# Vendor unit ships --noplugin=wifi; on kugo the wifi plugin must run
# (bcmdhd + wpa_supplicant via nl80211). Override ExecStart without it.
[Service]
ExecStart=
ExecStart=/usr/sbin/connmand -n -W nl80211 --nobacktrace $SYSCONF_ARGS $CONNMAN_ARGS
