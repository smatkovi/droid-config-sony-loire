# kugo: pass chip config directly (EnvironmentFile CONFIGFILE
# word-splitting was unreliable). Requires bt-macaddr.service.
[Unit]
After=bt-macaddr.service
[Service]
ExecStart=
ExecStart=/usr/sbin/bluetooth_rfkill_event -c /etc/bluetooth-rfkill-event/bcm4345c0.conf
