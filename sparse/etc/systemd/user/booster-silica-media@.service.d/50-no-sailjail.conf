# kugo: kernel lacks CONFIG_USER_NS -> firejail clone() EINVAL.
# Launch booster without sailjail until kernel rebuild.
[Service]
ExecStart=
ExecStart=/usr/bin/invoker --type=silica-session -- /usr/libexec/mapplauncherd/booster-silica-media --application=%i
